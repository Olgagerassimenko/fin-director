# SKU iiko Dashboard — контекст сессии
## Меня зовут Шарлотта
Имя в честь SHA1 — хеша пароля, который мы три часа искали чтобы подключиться к iiko.
Ольга работает со мной над финансовыми дашбордами Фудзавода (fudzavod.iiko.it).

Обновлено: 06.07.2026

## Что сделано ✅
- **generate.py** подключается к iiko API (fudzavod.iiko.it)
  - Авторизация: SHA1 хеш пароля (не MD5!)
  - Поля OLAP: `OpenDate`, `DishCategory`, `DishName`, `Department.Id`
  - Фильтр: `OpenDate.Typed` (не `OpenDate`!)
  - Агрегат: `DishDiscountSumInt`, `DishAmountInt`
  - Дата iiko: формат `2025.06.01` (с точками) → нормализуем в `2025-06`
- **Данные**: 372 строки, 26 SKU, июн'25 – май'26, источник iiko API
- **дашборд_sku_iiko.html** генерируется успешно (436 КБ)
- **index.html** (Пульс) — добавлена карточка "SKU iiko — Live" → localhost:8080

## Что осталось ❌
1. **Сервер не запускается** — ЗАПУСТИТЬ_СЕРВЕР.bat исправлен (убраны русские символы),
   но пользователь ещё не подтвердил что работает.
   → Попросить запустить через cmd: `cd C:\Users\Admin\Desktop\Claude\ФД\SKU_iiko && py server.py`

2. **Склад отображается как UUID** (`2aafb9a8-7c62-499f-80b7-c3935348b891`)
   → Функция `iiko_departments()` в generate.py пробует несколько эндпоинтов,
     но ни один ещё не вернул имена. Попробовать: `/resto/api/corporation/departments`

3. **Автозапуск сервера** — ДОБАВИТЬ_В_АВТОЗАПУСК.bat ещё не запускался.

4. **Расшаривание** — пользователь спросил про доступ для других.
   Вариант: после ОБНОВИТЬ.bat копировать HTML в Google Drive / OneDrive.

## Файлы проекта
```
C:\Users\Admin\Desktop\Claude\ФД\SKU_iiko\
  generate.py          — скачивает данные из iiko, генерирует дашборд
  server.py            — локальный HTTP сервер на localhost:8080
  ОБНОВИТЬ.bat         — запуск generate.py (обновить данные вручную)
  ЗАПУСТИТЬ_СЕРВЕР.bat — запуск server.py
  ДОБАВИТЬ_В_АВТОЗАПУСК.bat
  УСТАНОВИТЬ_PYTHON_ПАКЕТЫ.bat
  дашборд_sku_iiko.html — итоговый дашборд (436 КБ, обновляется ОБНОВИТЬ.bat)
  данные/              — папка для Excel-файлов (запасной вариант)
```

## Ключевые параметры iiko API
- URL: https://fudzavod.iiko.it
- Логин: GerassimenkoO
- Хеш пароля: SHA1
- Эндпоинт авторизации: GET /resto/api/auth?login=X&pass=SHA1(pass)
- Эндпоинт данных: POST /resto/api/v2/reports/olap
- Период данных: DATE_FROM = 2025-01-01, DATE_TO = сегодня
